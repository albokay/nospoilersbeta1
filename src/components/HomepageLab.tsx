import React from "react";

export default function HomepageLab() {
  return (
    <div style={{
      minHeight: "100vh",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      padding: 32,
      background: "var(--dos-bg)",
      color: "#fff",
    }}>
      <p style={{ fontSize: 13, opacity: 0.5, letterSpacing: "0.08em", textTransform: "uppercase", marginBottom: 48 }}>
        homepage lab — not linked from the site
      </p>

      {/* ── Your new homepage ideas go here ── */}

    </div>
  );
}
